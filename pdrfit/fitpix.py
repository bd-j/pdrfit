

def load_observations_fromtxt(filename):
    return obs_structured_array

def load_observations_fromfits(files, fields):
    return obs_structured_array


def fit_pixel(obs, model, priors=None):
    return theta, lnprob


def sample_priors(prior_desc):
    priors=[]
    for par, prior in prior_desc.iteritems():
        
    return theta, theta_labels
